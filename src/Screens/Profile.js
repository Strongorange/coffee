import React from "react";
import styled from "styled-components";

const Article = styled.article``;

const NormalText = styled.span``;

const Profile = () => {
  return (
    <Article>
      <NormalText>It's Profile</NormalText>
    </Article>
  );
};

export default Profile;
